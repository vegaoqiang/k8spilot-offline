{
  "signing": {
    "default": {
      "expiry": "876000h"
    },
    "profiles": {
      "kubernetes": {
        "usages": ["signing", "key encipherment", "client auth", "server auth"],
        "expiry": "876000h"
      },
      "etcd": {
        "usages": ["signing", "key encipherment", "client auth", "server auth"],
        "expiry": "87600h"
      }
    }
  }
}